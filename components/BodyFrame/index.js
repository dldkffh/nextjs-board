import Box from "@mui/material/Box";
import { styled } from "@mui/material/styles";

const BodyFrame = styled(Box)({
   width: {
      sx: "100%",
      sm: "100%",
      md: "100%",
      lg: "1200px",
      xl: "1200px",
   },
});

export default BodyFrame;

